# wow-addon-LibPrettyPrint
LibPrettyPrint is a lightweight World of Warcraft utility library for readable, safe table inspection and debugging. It pretty-prints Lua values with configurable depth limits, key sorting, and formatting, handles cyclic tables safely, and is optimized for dev/debug use without impacting runtime performance.
